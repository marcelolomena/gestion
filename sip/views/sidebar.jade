#sidebar
  .perfil_sesion
    .user
     #p #{JSON.stringify(user[0].nombre)}
    .usuario1 
    .usuario3
      a(href='signout') Cerrar Sesión
  #cssmenu
    ul
      each val in user[1].menus
        li.has-sub
          a(href='#')
            span=val.menu.menu
          ul
            each vol in val.submenu
              li
                a(href='#')
                  span=vol.opt
          