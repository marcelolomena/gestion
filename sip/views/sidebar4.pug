#sidebar
  .perfil_sesion
    .user
     #p #{data[0].nombre}
    .usuario1 
     if(data[0].contadorPreguntas>0)
      #p
       a(style='color:orange;',href='/sic/inboxpreguntas') Tiene #{data[0].contadorPreguntas} pregunta sin responder 
    .usuario3
      a(href='/menu/signout') Cerrar Sesión
  #cssmenu
    ul
      each val in data[1].menus
        li.has-sub
          a(href='#')
            span=val.menu.menu
          ul
            each vol in val.submenu
              li
                if(vol.url!='limites')
                  a(href='/'+vol.url)
                    span=vol.opt
                else                    
                  a(href='http://152.139.49.217:8000/miplataforma-web/centro-de-negocios/')
                    span=vol.opt                